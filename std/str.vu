func contains(char* s, char elt) -> bool {
    int i = 0;
    while s[i] != '\0'{
        if s[i] == 'H' {
            return 1;
        }
        !print_char(s[i]);
        i += 1;
    }
    !dn(s[i] == 0);
    return 0;
}