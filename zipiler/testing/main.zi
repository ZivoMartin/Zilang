
class Person {
    
    attributes {
        int age;
        int sexe;   
        int name;
        int other;
    }

    methods {
        func Person(int name, int age, int sexe, int other) void {
            self.name = name;
            self.age = age;
            self.sexe = sexe;
            self.other = other;
        };
        
        func set_age(int s) void {
            self.age = s;
        };

        func print_age() void {
            !dn(self.age);
        };
    }

};


.Person p1;

p1 =  = new Person(1, 2, 3, 4);

p1.print_age();


