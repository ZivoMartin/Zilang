
class Person {
    
    attributes{
        int age;
        char* name;
        int sexe;
        char test;
    }

    methods {
        func Person(int age, int sexe, char* name) void {
            self.age = age;
            self.sexe = sexe;
            self.name = name;
        };

        func get_age() int {
           return self.age;
        };

#        func get_name() char* {
#           return self.name;
#        };      
    }

};


char name[7] = "Martin\0";


.Person person;

# !dn(person.get_age());


!exit(0);