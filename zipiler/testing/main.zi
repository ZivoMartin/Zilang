$import <io>

class Person {
    
    attributes {
        int age;
        int sexe;   
        int name;
        int other;
    }

    methods {
        func Person() void {
            self.name = 10;
            self.age = 12;
            self.sexe = 5;
            self.other = 12;
        };
    }

};

int a;

.Person p1 = new Person();

!dn(p1.name);
!dn(p1.age);
!dn(p1.sexe);
!dn(p1.other); 

